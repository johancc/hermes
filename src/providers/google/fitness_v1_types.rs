#![allow(unused_variables, unused_mut, dead_code, non_camel_case_types)]
//! This file was generated by async-google-apis. (https://github.com/dermesser/async-google-apis)
//!
//! (c) 2020 Lewin Bormann <lbo@spheniscida.de>
//!
//! ## Getting started
//!
//! **Tip**: Take a look at those types ending in `...Service`. These represent API resources
//! and contain methods to interact with an API. The remaining types are used by those methods
//! and can be explored starting from a method you want to use.
//!
//! The generated code's dependencies are in the `async-google-apis-common` crate. The main
//! dependencies are hyper, yup-oauth2 (for OAuth authentication), and serde.
//!
//! I'd be happy if you let me know about your use case of this code.
//!
//! THIS FILE HAS BEEN GENERATED -- SAVE ANY MODIFICATIONS BEFORE REPLACING.

use async_google_apis_common::*;

/// Scopes of this API. Convertible to their string representation with `AsRef`.
#[derive(Debug, Clone, Copy)]
pub enum FitnessScopes {
    /// Use Google Fit to see and store your physical activity data
    ///
    /// URL: https://www.googleapis.com/auth/fitness.activity.read
    FitnessActivityRead,
    /// Add to your Google Fit physical activity data
    ///
    /// URL: https://www.googleapis.com/auth/fitness.activity.write
    FitnessActivityWrite,
    /// See info about your blood glucose in Google Fit. I consent to Google sharing my blood glucose information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.blood_glucose.read
    FitnessBloodGlucoseRead,
    /// Add info about your blood glucose to Google Fit. I consent to Google using my blood glucose information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.blood_glucose.write
    FitnessBloodGlucoseWrite,
    /// See info about your blood pressure in Google Fit. I consent to Google sharing my blood pressure information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.blood_pressure.read
    FitnessBloodPressureRead,
    /// Add info about your blood pressure in Google Fit. I consent to Google using my blood pressure information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.blood_pressure.write
    FitnessBloodPressureWrite,
    /// See info about your body measurements and heart rate in Google Fit
    ///
    /// URL: https://www.googleapis.com/auth/fitness.body.read
    FitnessBodyRead,
    /// Add info about your body measurements and heart rate to Google Fit
    ///
    /// URL: https://www.googleapis.com/auth/fitness.body.write
    FitnessBodyWrite,
    /// See info about your body temperature in Google Fit. I consent to Google sharing my body temperature information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.body_temperature.read
    FitnessBodyTemperatureRead,
    /// Add to info about your body temperature in Google Fit. I consent to Google using my body temperature information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.body_temperature.write
    FitnessBodyTemperatureWrite,
    /// See your heart rate data in Google Fit. I consent to Google sharing my heart rate information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.heart_rate.read
    FitnessHeartRateRead,
    /// Add to your heart rate data in Google Fit. I consent to Google using my heart rate information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.heart_rate.write
    FitnessHeartRateWrite,
    /// See your Google Fit speed and distance data
    ///
    /// URL: https://www.googleapis.com/auth/fitness.location.read
    FitnessLocationRead,
    /// Add to your Google Fit location data
    ///
    /// URL: https://www.googleapis.com/auth/fitness.location.write
    FitnessLocationWrite,
    /// See info about your nutrition in Google Fit
    ///
    /// URL: https://www.googleapis.com/auth/fitness.nutrition.read
    FitnessNutritionRead,
    /// Add to info about your nutrition in Google Fit
    ///
    /// URL: https://www.googleapis.com/auth/fitness.nutrition.write
    FitnessNutritionWrite,
    /// See info about your oxygen saturation in Google Fit. I consent to Google sharing my oxygen saturation information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.oxygen_saturation.read
    FitnessOxygenSaturationRead,
    /// Add info about your oxygen saturation in Google Fit. I consent to Google using my oxygen saturation information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.oxygen_saturation.write
    FitnessOxygenSaturationWrite,
    /// See info about your reproductive health in Google Fit. I consent to Google sharing my reproductive health information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.reproductive_health.read
    FitnessReproductiveHealthRead,
    /// Add info about your reproductive health in Google Fit. I consent to Google using my reproductive health information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.reproductive_health.write
    FitnessReproductiveHealthWrite,
    /// See your sleep data in Google Fit. I consent to Google sharing my sleep information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.sleep.read
    FitnessSleepRead,
    /// Add to your sleep data in Google Fit. I consent to Google using my sleep information with this app.
    ///
    /// URL: https://www.googleapis.com/auth/fitness.sleep.write
    FitnessSleepWrite,
}

impl std::convert::AsRef<str> for FitnessScopes {
    fn as_ref(&self) -> &'static str {
        match self {
            FitnessScopes::FitnessActivityRead => {
                "https://www.googleapis.com/auth/fitness.activity.read"
            }
            FitnessScopes::FitnessActivityWrite => {
                "https://www.googleapis.com/auth/fitness.activity.write"
            }
            FitnessScopes::FitnessBloodGlucoseRead => {
                "https://www.googleapis.com/auth/fitness.blood_glucose.read"
            }
            FitnessScopes::FitnessBloodGlucoseWrite => {
                "https://www.googleapis.com/auth/fitness.blood_glucose.write"
            }
            FitnessScopes::FitnessBloodPressureRead => {
                "https://www.googleapis.com/auth/fitness.blood_pressure.read"
            }
            FitnessScopes::FitnessBloodPressureWrite => {
                "https://www.googleapis.com/auth/fitness.blood_pressure.write"
            }
            FitnessScopes::FitnessBodyRead => "https://www.googleapis.com/auth/fitness.body.read",
            FitnessScopes::FitnessBodyWrite => "https://www.googleapis.com/auth/fitness.body.write",
            FitnessScopes::FitnessBodyTemperatureRead => {
                "https://www.googleapis.com/auth/fitness.body_temperature.read"
            }
            FitnessScopes::FitnessBodyTemperatureWrite => {
                "https://www.googleapis.com/auth/fitness.body_temperature.write"
            }
            FitnessScopes::FitnessHeartRateRead => {
                "https://www.googleapis.com/auth/fitness.heart_rate.read"
            }
            FitnessScopes::FitnessHeartRateWrite => {
                "https://www.googleapis.com/auth/fitness.heart_rate.write"
            }
            FitnessScopes::FitnessLocationRead => {
                "https://www.googleapis.com/auth/fitness.location.read"
            }
            FitnessScopes::FitnessLocationWrite => {
                "https://www.googleapis.com/auth/fitness.location.write"
            }
            FitnessScopes::FitnessNutritionRead => {
                "https://www.googleapis.com/auth/fitness.nutrition.read"
            }
            FitnessScopes::FitnessNutritionWrite => {
                "https://www.googleapis.com/auth/fitness.nutrition.write"
            }
            FitnessScopes::FitnessOxygenSaturationRead => {
                "https://www.googleapis.com/auth/fitness.oxygen_saturation.read"
            }
            FitnessScopes::FitnessOxygenSaturationWrite => {
                "https://www.googleapis.com/auth/fitness.oxygen_saturation.write"
            }
            FitnessScopes::FitnessReproductiveHealthRead => {
                "https://www.googleapis.com/auth/fitness.reproductive_health.read"
            }
            FitnessScopes::FitnessReproductiveHealthWrite => {
                "https://www.googleapis.com/auth/fitness.reproductive_health.write"
            }
            FitnessScopes::FitnessSleepRead => "https://www.googleapis.com/auth/fitness.sleep.read",
            FitnessScopes::FitnessSleepWrite => {
                "https://www.googleapis.com/auth/fitness.sleep.write"
            }
        }
    }
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct AggregateBucket {
    /// Available for Bucket.Type.ACTIVITY_TYPE, Bucket.Type.ACTIVITY_SEGMENT
    #[serde(rename = "activity")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub activity: Option<i32>,
    /// There will be one dataset per AggregateBy in the request.
    #[serde(rename = "dataset")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub dataset: Option<Vec<Dataset>>,
    /// i64: The end time for the aggregated data, in milliseconds since epoch, inclusive.
    #[serde(rename = "endTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub end_time_millis: Option<String>,
    #[serde(rename = "session")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub session: Option<Session>,
    /// i64: The start time for the aggregated data, in milliseconds since epoch, inclusive.
    #[serde(rename = "startTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub start_time_millis: Option<String>,
    /// The type of a bucket signifies how the data aggregation is performed in the bucket.
    #[serde(rename = "type")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub type_: Option<AggregateBucketType>,
}

/// The specification of which data to aggregate.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct AggregateBy {
    /// A data source ID to aggregate. Only data from the specified data source ID will be included in the aggregation. If specified, this data source must exist; the OAuth scopes in the supplied credentials must grant read access to this data type. The dataset in the response will have the same data source ID. Note: Data can be aggregated by either the dataTypeName or the dataSourceId, not both.
    #[serde(rename = "dataSourceId")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_source_id: Option<String>,
    /// The data type to aggregate. All data sources providing this data type will contribute data to the aggregation. The response will contain a single dataset for this data type name. The dataset will have a data source ID of derived::com.google.android.gms:aggregated. If the user has no data for this data type, an empty data set will be returned. Note: Data can be aggregated by either the dataTypeName or the dataSourceId, not both.
    #[serde(rename = "dataTypeName")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_type_name: Option<String>,
}

/// Next id: 10
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct AggregateRequest {
    /// The specification of data to be aggregated. At least one aggregateBy spec must be provided. All data that is specified will be aggregated using the same bucketing criteria. There will be one dataset in the response for every aggregateBy spec.
    #[serde(rename = "aggregateBy")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub aggregate_by: Option<Vec<AggregateBy>>,
    #[serde(rename = "bucketByActivitySegment")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub bucket_by_activity_segment: Option<BucketByActivity>,
    #[serde(rename = "bucketByActivityType")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub bucket_by_activity_type: Option<BucketByActivity>,
    #[serde(rename = "bucketBySession")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub bucket_by_session: Option<BucketBySession>,
    #[serde(rename = "bucketByTime")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub bucket_by_time: Option<BucketByTime>,
    /// i64: The end of a window of time. Data that intersects with this time window will be aggregated. The time is in milliseconds since epoch, inclusive.
    #[serde(rename = "endTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub end_time_millis: Option<String>,
    /// DO NOT POPULATE THIS FIELD. It is ignored.
    #[serde(rename = "filteredDataQualityStandard")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub filtered_data_quality_standard: Option<Vec<AggregateRequestFilteredDataQualityStandard>>,
    /// i64: The start of a window of time. Data that intersects with this time window will be aggregated. The time is in milliseconds since epoch, inclusive.
    #[serde(rename = "startTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub start_time_millis: Option<String>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct AggregateResponse {
    /// A list of buckets containing the aggregated data.
    #[serde(rename = "bucket")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub bucket: Option<Vec<AggregateBucket>>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct Application {
    /// An optional URI that can be used to link back to the application.
    #[serde(rename = "detailsUrl")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub details_url: Option<String>,
    /// The name of this application. This is required for REST clients, but we do not enforce uniqueness of this name. It is provided as a matter of convenience for other developers who would like to identify which REST created an Application or Data Source.
    #[serde(rename = "name")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Package name for this application. This is used as a unique identifier when created by Android applications, but cannot be specified by REST clients. REST clients will have their developer project number reflected into the Data Source data stream IDs, instead of the packageName.
    #[serde(rename = "packageName")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub package_name: Option<String>,
    /// Version of the application. You should update this field whenever the application changes in a way that affects the computation of the data.
    #[serde(rename = "version")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub version: Option<String>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct BucketByActivity {
    /// The default activity stream will be used if a specific activityDataSourceId is not specified.
    #[serde(rename = "activityDataSourceId")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub activity_data_source_id: Option<String>,
    /// i64: Specifies that only activity segments of duration longer than minDurationMillis are considered and used as a container for aggregated data.
    #[serde(rename = "minDurationMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub min_duration_millis: Option<String>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct BucketBySession {
    /// i64: Specifies that only sessions of duration longer than minDurationMillis are considered and used as a container for aggregated data.
    #[serde(rename = "minDurationMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub min_duration_millis: Option<String>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct BucketByTime {
    /// i64: Specifies that result buckets aggregate data by exactly durationMillis time frames. Time frames that contain no data will be included in the response with an empty dataset.
    #[serde(rename = "durationMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub duration_millis: Option<String>,
    #[serde(rename = "period")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub period: Option<BucketByTimePeriod>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct BucketByTimePeriod {
    /// org.joda.timezone.DateTimeZone
    #[serde(rename = "timeZoneId")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub time_zone_id: Option<String>,
    #[serde(rename = "type")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub type_: Option<BucketByTimePeriodType>,
    #[serde(rename = "value")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub value: Option<i32>,
}

/// Represents a single data point, generated by a particular data source. A data point holds a value for each field, an end timestamp and an optional start time. The exact semantics of each of these attributes are specified in the documentation for the particular data type. A data point can represent an instantaneous measurement, reading or input observation, as well as averages or aggregates over a time interval. Check the data type documentation to determine which is the case for a particular data type. Data points always contain one value for each field of the data type.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct DataPoint {
    /// i64: DO NOT USE THIS FIELD. It is ignored, and not stored.
    #[serde(rename = "computationTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub computation_time_millis: Option<String>,
    /// The data type defining the format of the values in this data point.
    #[serde(rename = "dataTypeName")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_type_name: Option<String>,
    /// i64: The end time of the interval represented by this data point, in nanoseconds since epoch.
    #[serde(rename = "endTimeNanos")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub end_time_nanos: Option<String>,
    /// i64: Indicates the last time this data point was modified. Useful only in contexts where we are listing the data changes, rather than representing the current state of the data.
    #[serde(rename = "modifiedTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub modified_time_millis: Option<String>,
    /// If the data point is contained in a dataset for a derived data source, this field will be populated with the data source stream ID that created the data point originally. WARNING: do not rely on this field for anything other than debugging. The value of this field, if it is set at all, is an implementation detail and is not guaranteed to remain consistent.
    #[serde(rename = "originDataSourceId")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub origin_data_source_id: Option<String>,
    /// i64: The raw timestamp from the original SensorEvent.
    #[serde(rename = "rawTimestampNanos")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub raw_timestamp_nanos: Option<String>,
    /// i64: The start time of the interval represented by this data point, in nanoseconds since epoch.
    #[serde(rename = "startTimeNanos")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub start_time_nanos: Option<String>,
    /// Values of each data type field for the data point. It is expected that each value corresponding to a data type field will occur in the same order that the field is listed with in the data type specified in a data source. Only one of integer and floating point fields will be populated, depending on the format enum value within data source's type field.
    #[serde(rename = "value")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub value: Option<Vec<Value>>,
}

/// Definition of a unique source of sensor data. Data sources can expose raw data coming from hardware sensors on local or companion devices. They can also expose derived data, created by transforming or merging other data sources. Multiple data sources can exist for the same data type. Every data point inserted into or read from this service has an associated data source. The data source contains enough information to uniquely identify its data, including the hardware device and the application that collected and/or transformed the data. It also holds useful metadata, such as the hardware and application versions, and the device type. Each data source produces a unique stream of data, with a unique identifier. Not all changes to data source affect the stream identifier, so that data collected by updated versions of the same application/device can still be considered to belong to the same data stream.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct DataSource {
    #[serde(rename = "application")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub application: Option<Application>,
    /// DO NOT POPULATE THIS FIELD. It is never populated in responses from the platform, and is ignored in queries. It will be removed in a future version entirely.
    #[serde(rename = "dataQualityStandard")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_quality_standard: Option<Vec<DataSourceDataQualityStandard>>,
    /// A unique identifier for the data stream produced by this data source. The identifier includes: - The physical device's manufacturer, model, and serial number (UID). - The application's package name or name. Package name is used when the data source was created by an Android application. The developer project number is used when the data source was created by a REST client. - The data source's type. - The data source's stream name. Note that not all attributes of the data source are used as part of the stream identifier. In particular, the version of the hardware/the application isn't used. This allows us to preserve the same stream through version updates. This also means that two DataSource objects may represent the same data stream even if they're not equal. The exact format of the data stream ID created by an Android application is: type:dataType.name:application.packageName:device.manufacturer:device.model:device.uid:dataStreamName The exact format of the data stream ID created by a REST client is: type:dataType.name:developer project number:device.manufacturer:device.model:device.uid:dataStreamName When any of the optional fields that make up the data stream ID are absent, they will be omitted from the data stream ID. The minimum viable data stream ID would be: type:dataType.name:developer project number Finally, the developer project number and device UID are obfuscated when read by any REST or Android client that did not create the data source. Only the data source creator will see the developer project number in clear and normal form. This means a client will see a different set of data_stream_ids than another client with different credentials.
    #[serde(rename = "dataStreamId")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_stream_id: Option<String>,
    /// The stream name uniquely identifies this particular data source among other data sources of the same type from the same underlying producer. Setting the stream name is optional, but should be done whenever an application exposes two streams for the same data type, or when a device has two equivalent sensors.
    #[serde(rename = "dataStreamName")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_stream_name: Option<String>,
    #[serde(rename = "dataType")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_type: Option<DataType>,
    #[serde(rename = "device")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub device: Option<Device>,
    /// An end-user visible name for this data source.
    #[serde(rename = "name")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// A constant describing the type of this data source. Indicates whether this data source produces raw or derived data.
    #[serde(rename = "type")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub type_: Option<DataSourceType>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct DataType {
    /// A field represents one dimension of a data type.
    #[serde(rename = "field")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub field: Option<Vec<Box<DataTypeField>>>,
    /// Each data type has a unique, namespaced, name. All data types in the com.google namespace are shared as part of the platform.
    #[serde(rename = "name")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

/// In case of multi-dimensional data (such as an accelerometer with x, y, and z axes) each field represents one dimension. Each data type field has a unique name which identifies it. The field also defines the format of the data (int, float, etc.). This message is only instantiated in code and not used for wire comms or stored in any way.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct DataTypeField {
    /// The different supported formats for each field in a data type.
    #[serde(rename = "format")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub format: Option<DataTypeFieldFormat>,
    /// Defines the name and format of data. Unlike data type names, field names are not namespaced, and only need to be unique within the data type.
    #[serde(rename = "name")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    #[serde(rename = "optional")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub optional: Option<bool>,
}

/// A dataset represents a projection container for data points. They do not carry any info of their own. Datasets represent a set of data points from a particular data source. A data point can be found in more than one dataset.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct Dataset {
    /// The data stream ID of the data source that created the points in this dataset.
    #[serde(rename = "dataSourceId")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_source_id: Option<String>,
    /// i64: The largest end time of all data points in this possibly partial representation of the dataset. Time is in nanoseconds from epoch. This should also match the second part of the dataset identifier.
    #[serde(rename = "maxEndTimeNs")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub max_end_time_ns: Option<String>,
    /// i64: The smallest start time of all data points in this possibly partial representation of the dataset. Time is in nanoseconds from epoch. This should also match the first part of the dataset identifier.
    #[serde(rename = "minStartTimeNs")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub min_start_time_ns: Option<String>,
    /// This token will be set when a dataset is received in response to a GET request and the dataset is too large to be included in a single response. Provide this value in a subsequent GET request to return the next page of data points within this dataset.
    #[serde(rename = "nextPageToken")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// A partial list of data points contained in the dataset, ordered by endTimeNanos. This list is considered complete when retrieving a small dataset and partial when patching a dataset or retrieving a dataset that is too large to include in a single response.
    #[serde(rename = "point")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub point: Option<Vec<DataPoint>>,
}

/// Representation of an integrated device (such as a phone or a wearable) that can hold sensors. Each sensor is exposed as a data source. The main purpose of the device information contained in this class is to identify the hardware of a particular data source. This can be useful in different ways, including: - Distinguishing two similar sensors on different devices (the step counter on two nexus 5 phones, for instance) - Display the source of data to the user (by using the device make / model) - Treat data differently depending on sensor type (accelerometers on a watch may give different patterns than those on a phone) - Build different analysis models for each device/version.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct Device {
    /// Manufacturer of the product/hardware.
    #[serde(rename = "manufacturer")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub manufacturer: Option<String>,
    /// End-user visible model name for the device.
    #[serde(rename = "model")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub model: Option<String>,
    /// A constant representing the type of the device.
    #[serde(rename = "type")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub type_: Option<DeviceType>,
    /// The serial number or other unique ID for the hardware. This field is obfuscated when read by any REST or Android client that did not create the data source. Only the data source creator will see the uid field in clear and normal form. The obfuscation preserves equality; that is, given two IDs, if id1 == id2, obfuscated(id1) == obfuscated(id2).
    #[serde(rename = "uid")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub uid: Option<String>,
    /// Version string for the device hardware/software.
    #[serde(rename = "version")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub version: Option<String>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct ListDataPointChangesResponse {
    /// The data stream ID of the data source with data point changes.
    #[serde(rename = "dataSourceId")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_source_id: Option<String>,
    /// Deleted data points for the user. Note, for modifications this should be parsed before handling insertions.
    #[serde(rename = "deletedDataPoint")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_data_point: Option<Vec<DataPoint>>,
    /// Inserted data points for the user.
    #[serde(rename = "insertedDataPoint")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub inserted_data_point: Option<Vec<DataPoint>>,
    /// The continuation token, which is used to page through large result sets. Provide this value in a subsequent request to return the next page of results.
    #[serde(rename = "nextPageToken")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct ListDataSourcesResponse {
    /// A previously created data source.
    #[serde(rename = "dataSource")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub data_source: Option<Vec<DataSource>>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct ListSessionsResponse {
    /// If includeDeleted is set to true in the request, and startTime and endTime are omitted, this will include sessions which were deleted since the last sync.
    #[serde(rename = "deletedSession")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_session: Option<Vec<Session>>,
    /// Flag to indicate server has more data to transfer. DO NOT USE THIS FIELD. It is never populated in responses from the server.
    #[serde(rename = "hasMoreData")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub has_more_data: Option<bool>,
    /// The sync token which is used to sync further changes. This will only be provided if both startTime and endTime are omitted from the request.
    #[serde(rename = "nextPageToken")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Sessions with an end time that is between startTime and endTime of the request.
    #[serde(rename = "session")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub session: Option<Vec<Session>>,
}

/// Holder object for the value of an entry in a map field of a data point. A map value supports a subset of the formats that the regular Value supports.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct MapValue {
    /// Floating point value.
    #[serde(rename = "fpVal")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub fp_val: Option<f64>,
}

/// Sessions contain metadata, such as a user-friendly name and time interval information.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct Session {
    /// i64: Session active time. While start_time_millis and end_time_millis define the full session time, the active time can be shorter and specified by active_time_millis. If the inactive time during the session is known, it should also be inserted via a com.google.activity.segment data point with a STILL activity value
    #[serde(rename = "activeTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub active_time_millis: Option<String>,
    /// The type of activity this session represents.
    #[serde(rename = "activityType")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub activity_type: Option<i32>,
    #[serde(rename = "application")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub application: Option<Application>,
    /// A description for this session.
    #[serde(rename = "description")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// i64: An end time, in milliseconds since epoch, inclusive.
    #[serde(rename = "endTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub end_time_millis: Option<String>,
    /// A client-generated identifier that is unique across all sessions owned by this particular user.
    #[serde(rename = "id")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// i64: A timestamp that indicates when the session was last modified.
    #[serde(rename = "modifiedTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub modified_time_millis: Option<String>,
    /// A human readable name of the session.
    #[serde(rename = "name")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// i64: A start time, in milliseconds since epoch, inclusive.
    #[serde(rename = "startTimeMillis")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub start_time_millis: Option<String>,
}

/// Holder object for the value of a single field in a data point. A field value has a particular format and is only ever set to one of an integer or a floating point value.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct Value {
    /// Floating point value. When this is set, other values must not be set.
    #[serde(rename = "fpVal")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub fp_val: Option<f64>,
    /// Integer value. When this is set, other values must not be set.
    #[serde(rename = "intVal")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub int_val: Option<i32>,
    /// Map value. The valid key space and units for the corresponding value of each entry should be documented as part of the data type definition. Keys should be kept small whenever possible. Data streams with large keys and high data frequency may be down sampled.
    #[serde(rename = "mapVal")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub map_val: Option<Vec<ValueMapValEntry>>,
    /// String value. When this is set, other values must not be set. Strings should be kept small whenever possible. Data streams with large string values and high data frequency may be down sampled.
    #[serde(rename = "stringVal")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub string_val: Option<String>,
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct ValueMapValEntry {
    #[serde(rename = "key")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    #[serde(rename = "value")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub value: Option<MapValue>,
}

#[derive(Debug, Clone, Copy, Serialize, Deserialize)]
pub enum AggregateBucketType {
    Undefined,
    #[serde(rename = "activitySegment")]
    ActivitySegment,
    /// Denotes that bucketing by time is requested. When this is specified, the timeBucketDurationMillis field is used to determine how many buckets will be returned.
    #[serde(rename = "activityType")]
    ActivityType,
    /// Denotes that bucketing by session is requested. When this is specified, only data that occurs within sessions that begin and end within the dataset time frame, is included in the results.
    #[serde(rename = "session")]
    Session,
    /// Denotes that bucketing by activity type is requested. When this is specified, there will be one bucket for each unique activity type that a user participated in, during the dataset time frame of interest.
    #[serde(rename = "time")]
    Time,
    /// Denotes that bucketing by individual activity segment is requested. This will aggregate data by the time boundaries specified by each activity segment occurring within the dataset time frame of interest.
    #[serde(rename = "unknown")]
    Unknown,
}

impl std::default::Default for AggregateBucketType {
    fn default() -> AggregateBucketType {
        AggregateBucketType::Undefined
    }
}

impl std::fmt::Display for AggregateBucketType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let s = match self {
            AggregateBucketType::Undefined => write!(f, "undefined"),
            AggregateBucketType::ActivitySegment => write!(f, "activitySegment"),
            AggregateBucketType::ActivityType => write!(f, "activityType"),
            AggregateBucketType::Session => write!(f, "session"),
            AggregateBucketType::Time => write!(f, "time"),
            AggregateBucketType::Unknown => write!(f, "unknown"),
        };
        Ok(())
    }
}

#[derive(Debug, Clone, Copy, Serialize, Deserialize)]
pub enum AggregateRequestFilteredDataQualityStandard {
    Undefined,
    #[serde(rename = "dataQualityBloodGlucoseIso151972003")]
    DataQualityBloodGlucoseIso151972003,
    #[serde(rename = "dataQualityBloodGlucoseIso151972013")]
    DataQualityBloodGlucoseIso151972013,
    #[serde(rename = "dataQualityBloodPressureAami")]
    DataQualityBloodPressureAami,
    #[serde(rename = "dataQualityBloodPressureBhsAA")]
    DataQualityBloodPressureBhsAA,
    #[serde(rename = "dataQualityBloodPressureBhsAB")]
    DataQualityBloodPressureBhsAB,
    #[serde(rename = "dataQualityBloodPressureBhsBA")]
    DataQualityBloodPressureBhsBA,
    #[serde(rename = "dataQualityBloodPressureBhsBB")]
    DataQualityBloodPressureBhsBB,
    #[serde(rename = "dataQualityBloodPressureEsh2002")]
    DataQualityBloodPressureEsh2002,
    #[serde(rename = "dataQualityBloodPressureEsh2010")]
    DataQualityBloodPressureEsh2010,
    #[serde(rename = "dataQualityUnknown")]
    DataQualityUnknown,
}

impl std::default::Default for AggregateRequestFilteredDataQualityStandard {
    fn default() -> AggregateRequestFilteredDataQualityStandard {
        AggregateRequestFilteredDataQualityStandard::Undefined
    }
}

impl std::fmt::Display for AggregateRequestFilteredDataQualityStandard {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let s = match self {
            AggregateRequestFilteredDataQualityStandard::Undefined => write!(f, "undefined"),
            AggregateRequestFilteredDataQualityStandard::DataQualityBloodGlucoseIso151972003 => {
                write!(f, "dataQualityBloodGlucoseIso151972003")
            }
            AggregateRequestFilteredDataQualityStandard::DataQualityBloodGlucoseIso151972013 => {
                write!(f, "dataQualityBloodGlucoseIso151972013")
            }
            AggregateRequestFilteredDataQualityStandard::DataQualityBloodPressureAami => {
                write!(f, "dataQualityBloodPressureAami")
            }
            AggregateRequestFilteredDataQualityStandard::DataQualityBloodPressureBhsAA => {
                write!(f, "dataQualityBloodPressureBhsAA")
            }
            AggregateRequestFilteredDataQualityStandard::DataQualityBloodPressureBhsAB => {
                write!(f, "dataQualityBloodPressureBhsAB")
            }
            AggregateRequestFilteredDataQualityStandard::DataQualityBloodPressureBhsBA => {
                write!(f, "dataQualityBloodPressureBhsBA")
            }
            AggregateRequestFilteredDataQualityStandard::DataQualityBloodPressureBhsBB => {
                write!(f, "dataQualityBloodPressureBhsBB")
            }
            AggregateRequestFilteredDataQualityStandard::DataQualityBloodPressureEsh2002 => {
                write!(f, "dataQualityBloodPressureEsh2002")
            }
            AggregateRequestFilteredDataQualityStandard::DataQualityBloodPressureEsh2010 => {
                write!(f, "dataQualityBloodPressureEsh2010")
            }
            AggregateRequestFilteredDataQualityStandard::DataQualityUnknown => {
                write!(f, "dataQualityUnknown")
            }
        };
        Ok(())
    }
}

#[derive(Debug, Clone, Copy, Serialize, Deserialize)]
pub enum BucketByTimePeriodType {
    Undefined,
    #[serde(rename = "day")]
    Day,
    #[serde(rename = "month")]
    Month,
    #[serde(rename = "week")]
    Week,
}

impl std::default::Default for BucketByTimePeriodType {
    fn default() -> BucketByTimePeriodType {
        BucketByTimePeriodType::Undefined
    }
}

impl std::fmt::Display for BucketByTimePeriodType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let s = match self {
            BucketByTimePeriodType::Undefined => write!(f, "undefined"),
            BucketByTimePeriodType::Day => write!(f, "day"),
            BucketByTimePeriodType::Month => write!(f, "month"),
            BucketByTimePeriodType::Week => write!(f, "week"),
        };
        Ok(())
    }
}

#[derive(Debug, Clone, Copy, Serialize, Deserialize)]
pub enum DataSourceDataQualityStandard {
    Undefined,
    #[serde(rename = "dataQualityBloodGlucoseIso151972003")]
    DataQualityBloodGlucoseIso151972003,
    #[serde(rename = "dataQualityBloodGlucoseIso151972013")]
    DataQualityBloodGlucoseIso151972013,
    #[serde(rename = "dataQualityBloodPressureAami")]
    DataQualityBloodPressureAami,
    #[serde(rename = "dataQualityBloodPressureBhsAA")]
    DataQualityBloodPressureBhsAA,
    #[serde(rename = "dataQualityBloodPressureBhsAB")]
    DataQualityBloodPressureBhsAB,
    #[serde(rename = "dataQualityBloodPressureBhsBA")]
    DataQualityBloodPressureBhsBA,
    #[serde(rename = "dataQualityBloodPressureBhsBB")]
    DataQualityBloodPressureBhsBB,
    #[serde(rename = "dataQualityBloodPressureEsh2002")]
    DataQualityBloodPressureEsh2002,
    #[serde(rename = "dataQualityBloodPressureEsh2010")]
    DataQualityBloodPressureEsh2010,
    #[serde(rename = "dataQualityUnknown")]
    DataQualityUnknown,
}

impl std::default::Default for DataSourceDataQualityStandard {
    fn default() -> DataSourceDataQualityStandard {
        DataSourceDataQualityStandard::Undefined
    }
}

impl std::fmt::Display for DataSourceDataQualityStandard {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let s = match self {
            DataSourceDataQualityStandard::Undefined => write!(f, "undefined"),
            DataSourceDataQualityStandard::DataQualityBloodGlucoseIso151972003 => {
                write!(f, "dataQualityBloodGlucoseIso151972003")
            }
            DataSourceDataQualityStandard::DataQualityBloodGlucoseIso151972013 => {
                write!(f, "dataQualityBloodGlucoseIso151972013")
            }
            DataSourceDataQualityStandard::DataQualityBloodPressureAami => {
                write!(f, "dataQualityBloodPressureAami")
            }
            DataSourceDataQualityStandard::DataQualityBloodPressureBhsAA => {
                write!(f, "dataQualityBloodPressureBhsAA")
            }
            DataSourceDataQualityStandard::DataQualityBloodPressureBhsAB => {
                write!(f, "dataQualityBloodPressureBhsAB")
            }
            DataSourceDataQualityStandard::DataQualityBloodPressureBhsBA => {
                write!(f, "dataQualityBloodPressureBhsBA")
            }
            DataSourceDataQualityStandard::DataQualityBloodPressureBhsBB => {
                write!(f, "dataQualityBloodPressureBhsBB")
            }
            DataSourceDataQualityStandard::DataQualityBloodPressureEsh2002 => {
                write!(f, "dataQualityBloodPressureEsh2002")
            }
            DataSourceDataQualityStandard::DataQualityBloodPressureEsh2010 => {
                write!(f, "dataQualityBloodPressureEsh2010")
            }
            DataSourceDataQualityStandard::DataQualityUnknown => write!(f, "dataQualityUnknown"),
        };
        Ok(())
    }
}

#[derive(Debug, Clone, Copy, Serialize, Deserialize)]
pub enum DataSourceType {
    Undefined,
    #[serde(rename = "derived")]
    Derived,
    #[serde(rename = "raw")]
    Raw,
}

impl std::default::Default for DataSourceType {
    fn default() -> DataSourceType {
        DataSourceType::Undefined
    }
}

impl std::fmt::Display for DataSourceType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let s = match self {
            DataSourceType::Undefined => write!(f, "undefined"),
            DataSourceType::Derived => write!(f, "derived"),
            DataSourceType::Raw => write!(f, "raw"),
        };
        Ok(())
    }
}

#[derive(Debug, Clone, Copy, Serialize, Deserialize)]
pub enum DataTypeFieldFormat {
    Undefined,
    #[serde(rename = "blob")]
    Blob,
    #[serde(rename = "floatList")]
    FloatList,
    #[serde(rename = "floatPoint")]
    FloatPoint,
    #[serde(rename = "integer")]
    Integer,
    #[serde(rename = "integerList")]
    IntegerList,
    #[serde(rename = "map")]
    Map,
    #[serde(rename = "string")]
    String,
}

impl std::default::Default for DataTypeFieldFormat {
    fn default() -> DataTypeFieldFormat {
        DataTypeFieldFormat::Undefined
    }
}

impl std::fmt::Display for DataTypeFieldFormat {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let s = match self {
            DataTypeFieldFormat::Undefined => write!(f, "undefined"),
            DataTypeFieldFormat::Blob => write!(f, "blob"),
            DataTypeFieldFormat::FloatList => write!(f, "floatList"),
            DataTypeFieldFormat::FloatPoint => write!(f, "floatPoint"),
            DataTypeFieldFormat::Integer => write!(f, "integer"),
            DataTypeFieldFormat::IntegerList => write!(f, "integerList"),
            DataTypeFieldFormat::Map => write!(f, "map"),
            DataTypeFieldFormat::String => write!(f, "string"),
        };
        Ok(())
    }
}

#[derive(Debug, Clone, Copy, Serialize, Deserialize)]
pub enum DeviceType {
    Undefined,
    /// Device type is not known.
    #[serde(rename = "chestStrap")]
    ChestStrap,
    /// An Android phone.
    #[serde(rename = "headMounted")]
    HeadMounted,
    /// An Android tablet.
    #[serde(rename = "phone")]
    Phone,
    /// A watch or other wrist-mounted band.
    #[serde(rename = "scale")]
    Scale,
    /// A chest strap.
    #[serde(rename = "smartDisplay")]
    SmartDisplay,
    /// A scale.
    #[serde(rename = "tablet")]
    Tablet,
    /// Glass or other head-mounted device.
    #[serde(rename = "unknown")]
    Unknown,
    /// A smart display e.g. Nest device.
    #[serde(rename = "watch")]
    Watch,
}

impl std::default::Default for DeviceType {
    fn default() -> DeviceType {
        DeviceType::Undefined
    }
}

impl std::fmt::Display for DeviceType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let s = match self {
            DeviceType::Undefined => write!(f, "undefined"),
            DeviceType::ChestStrap => write!(f, "chestStrap"),
            DeviceType::HeadMounted => write!(f, "headMounted"),
            DeviceType::Phone => write!(f, "phone"),
            DeviceType::Scale => write!(f, "scale"),
            DeviceType::SmartDisplay => write!(f, "smartDisplay"),
            DeviceType::Tablet => write!(f, "tablet"),
            DeviceType::Unknown => write!(f, "unknown"),
            DeviceType::Watch => write!(f, "watch"),
        };
        Ok(())
    }
}

#[derive(Debug, Clone, Copy, Serialize, Deserialize)]
pub enum FitnessParamsdollar_xgafv {
    Undefined,
    /// v1 error format
    #[serde(rename = "1")]
    _1,
    /// v2 error format
    #[serde(rename = "2")]
    _2,
}

impl std::default::Default for FitnessParamsdollar_xgafv {
    fn default() -> FitnessParamsdollar_xgafv {
        FitnessParamsdollar_xgafv::Undefined
    }
}

impl std::fmt::Display for FitnessParamsdollar_xgafv {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let s = match self {
            FitnessParamsdollar_xgafv::Undefined => write!(f, "undefined"),
            FitnessParamsdollar_xgafv::_1 => write!(f, "1"),
            FitnessParamsdollar_xgafv::_2 => write!(f, "2"),
        };
        Ok(())
    }
}

#[derive(Debug, Clone, Copy, Serialize, Deserialize)]
pub enum FitnessParamsAlt {
    Undefined,
    /// Responses with Content-Type of application/json
    #[serde(rename = "json")]
    Json,
    /// Media download with context-dependent Content-Type
    #[serde(rename = "media")]
    Media,
    /// Responses with Content-Type of application/x-protobuf
    #[serde(rename = "proto")]
    Proto,
}

impl std::default::Default for FitnessParamsAlt {
    fn default() -> FitnessParamsAlt {
        FitnessParamsAlt::Undefined
    }
}

impl std::fmt::Display for FitnessParamsAlt {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let s = match self {
            FitnessParamsAlt::Undefined => write!(f, "undefined"),
            FitnessParamsAlt::Json => write!(f, "json"),
            FitnessParamsAlt::Media => write!(f, "media"),
            FitnessParamsAlt::Proto => write!(f, "proto"),
        };
        Ok(())
    }
}

/// Parameters for the `dataSources.create` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDataSourcesCreateParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// Create the data source for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDataSourcesCreateParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        Ok(())
    }
}

/// Parameters for the `dataSources.delete` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDataSourcesDeleteParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The data stream ID of the data source to delete.
    #[serde(rename = "dataSourceId")]
    pub data_source_id: String,
    /// Retrieve a data source for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDataSourcesDeleteParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        Ok(())
    }
}

/// Parameters for the `dataSources.get` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDataSourcesGetParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The data stream ID of the data source to retrieve.
    #[serde(rename = "dataSourceId")]
    pub data_source_id: String,
    /// Retrieve a data source for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDataSourcesGetParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        Ok(())
    }
}

/// Parameters for the `dataSources.list` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDataSourcesListParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The names of data types to include in the list. If not specified, all data sources will be returned.
    #[serde(rename = "dataTypeName")]
    pub data_type_name: Option<String>,
    /// List data sources for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDataSourcesListParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        if let Some(ref v) = self.data_type_name {
            write!(
                f,
                "&dataTypeName={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        Ok(())
    }
}

/// Parameters for the `dataSources.update` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDataSourcesUpdateParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The data stream ID of the data source to update.
    #[serde(rename = "dataSourceId")]
    pub data_source_id: String,
    /// Update the data source for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDataSourcesUpdateParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        Ok(())
    }
}

/// Parameters for the `dataPointChanges.list` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDataSourcesDataPointChangesListParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The data stream ID of the data source that created the dataset.
    #[serde(rename = "dataSourceId")]
    pub data_source_id: String,
    /// If specified, no more than this many data point changes will be included in the response.
    #[serde(rename = "limit")]
    pub limit: Option<i32>,
    /// The continuation token, which is used to page through large result sets. To get the next page of results, set this parameter to the value of nextPageToken from the previous response.
    #[serde(rename = "pageToken")]
    pub page_token: Option<String>,
    /// List data points for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDataSourcesDataPointChangesListParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        if let Some(ref v) = self.limit {
            write!(
                f,
                "&limit={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.page_token {
            write!(
                f,
                "&pageToken={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        Ok(())
    }
}

/// Parameters for the `datasets.delete` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDataSourcesDatasetsDeleteParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The data stream ID of the data source that created the dataset.
    #[serde(rename = "dataSourceId")]
    pub data_source_id: String,
    /// Dataset identifier that is a composite of the minimum data point start time and maximum data point end time represented as nanoseconds from the epoch. The ID is formatted like: "startTime-endTime" where startTime and endTime are 64 bit integers.
    #[serde(rename = "datasetId")]
    pub dataset_id: String,
    /// Delete a dataset for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDataSourcesDatasetsDeleteParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        Ok(())
    }
}

/// Parameters for the `datasets.get` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDataSourcesDatasetsGetParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The data stream ID of the data source that created the dataset.
    #[serde(rename = "dataSourceId")]
    pub data_source_id: String,
    /// Dataset identifier that is a composite of the minimum data point start time and maximum data point end time represented as nanoseconds from the epoch. The ID is formatted like: "startTime-endTime" where startTime and endTime are 64 bit integers.
    #[serde(rename = "datasetId")]
    pub dataset_id: String,
    /// If specified, no more than this many data points will be included in the dataset. If there are more data points in the dataset, nextPageToken will be set in the dataset response. The limit is applied from the end of the time range. That is, if pageToken is absent, the limit most recent data points will be returned.
    #[serde(rename = "limit")]
    pub limit: Option<i32>,
    /// The continuation token, which is used to page through large datasets. To get the next page of a dataset, set this parameter to the value of nextPageToken from the previous response. Each subsequent call will yield a partial dataset with data point end timestamps that are strictly smaller than those in the previous partial response.
    #[serde(rename = "pageToken")]
    pub page_token: Option<String>,
    /// Retrieve a dataset for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDataSourcesDatasetsGetParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        if let Some(ref v) = self.limit {
            write!(
                f,
                "&limit={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.page_token {
            write!(
                f,
                "&pageToken={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        Ok(())
    }
}

/// Parameters for the `datasets.patch` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDataSourcesDatasetsPatchParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The data stream ID of the data source that created the dataset.
    #[serde(rename = "dataSourceId")]
    pub data_source_id: String,
    /// This field is not used, and can be safely omitted.
    #[serde(rename = "datasetId")]
    pub dataset_id: String,
    /// Patch a dataset for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDataSourcesDatasetsPatchParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        Ok(())
    }
}

/// Parameters for the `dataset.aggregate` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersDatasetAggregateParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// Aggregate data for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersDatasetAggregateParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        Ok(())
    }
}

/// Parameters for the `sessions.delete` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersSessionsDeleteParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The ID of the session to be deleted.
    #[serde(rename = "sessionId")]
    pub session_id: String,
    /// Delete a session for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersSessionsDeleteParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        Ok(())
    }
}

/// Parameters for the `sessions.list` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersSessionsListParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// If non-empty, only sessions with these activity types should be returned.
    #[serde(rename = "activityType")]
    pub activity_type: Option<i32>,
    /// An RFC3339 timestamp. Only sessions ending between the start and end times will be included in the response. If this time is omitted but startTime is specified, all sessions from startTime to the end of time will be returned.
    #[serde(rename = "endTime")]
    pub end_time: Option<String>,
    /// If true, and if both startTime and endTime are omitted, session deletions will be returned.
    #[serde(rename = "includeDeleted")]
    pub include_deleted: Option<bool>,
    /// The continuation token, which is used for incremental syncing. To get the next batch of changes, set this parameter to the value of nextPageToken from the previous response. The page token is ignored if either start or end time is specified. If none of start time, end time, and the page token is specified, sessions modified in the last 30 days are returned.
    #[serde(rename = "pageToken")]
    pub page_token: Option<String>,
    /// An RFC3339 timestamp. Only sessions ending between the start and end times will be included in the response. If this time is omitted but endTime is specified, all sessions from the start of time up to endTime will be returned.
    #[serde(rename = "startTime")]
    pub start_time: Option<String>,
    /// List sessions for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersSessionsListParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        if let Some(ref v) = self.activity_type {
            write!(
                f,
                "&activityType={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.end_time {
            write!(
                f,
                "&endTime={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.include_deleted {
            write!(
                f,
                "&includeDeleted={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.page_token {
            write!(
                f,
                "&pageToken={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.start_time {
            write!(
                f,
                "&startTime={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        Ok(())
    }
}

/// Parameters for the `sessions.update` method.
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct UsersSessionsUpdateParams {
    /// General attributes applying to any API call
    #[serde(flatten)]
    pub fitness_params: Option<FitnessParams>,
    /// The ID of the session to be created.
    #[serde(rename = "sessionId")]
    pub session_id: String,
    /// Create sessions for the person identified. Use me to indicate the authenticated user. Only me is supported at this time.
    #[serde(rename = "userId")]
    pub user_id: String,
}

impl std::fmt::Display for UsersSessionsUpdateParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        Ok(())
    }
}

#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct FitnessParams {
    /// V1 error format.
    #[serde(rename = "$.xgafv")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub dollar_xgafv: Option<FitnessParamsdollar_xgafv>,
    /// OAuth access token.
    #[serde(rename = "access_token")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub access_token: Option<String>,
    /// Data format for response.
    #[serde(rename = "alt")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub alt: Option<FitnessParamsAlt>,
    /// JSONP
    #[serde(rename = "callback")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub callback: Option<String>,
    /// Selector specifying which fields to include in a partial response.
    #[serde(rename = "fields")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub fields: Option<String>,
    /// API key. Your API key identifies your project and provides you with API access, quota, and reports. Required unless you provide an OAuth 2.0 token.
    #[serde(rename = "key")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    /// OAuth 2.0 token for the current user.
    #[serde(rename = "oauth_token")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub oauth_token: Option<String>,
    /// Returns response with indentations and line breaks.
    #[serde(rename = "prettyPrint")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub pretty_print: Option<bool>,
    /// Available to use for quota purposes for server-side applications. Can be any arbitrary string assigned to a user, but should not exceed 40 characters.
    #[serde(rename = "quotaUser")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub quota_user: Option<String>,
    /// Legacy upload protocol for media (e.g. "media", "multipart").
    #[serde(rename = "uploadType")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub upload_type: Option<String>,
    /// Upload protocol for media (e.g. "raw", "multipart").
    #[serde(rename = "upload_protocol")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub upload_protocol: Option<String>,
}

impl std::fmt::Display for FitnessParams {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        if let Some(ref v) = self.dollar_xgafv {
            write!(
                f,
                "&$.xgafv={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.access_token {
            write!(
                f,
                "&access_token={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.alt {
            write!(
                f,
                "&alt={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.callback {
            write!(
                f,
                "&callback={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.fields {
            write!(
                f,
                "&fields={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.key {
            write!(
                f,
                "&key={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.oauth_token {
            write!(
                f,
                "&oauth_token={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.pretty_print {
            write!(
                f,
                "&prettyPrint={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.quota_user {
            write!(
                f,
                "&quotaUser={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.upload_type {
            write!(
                f,
                "&uploadType={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        if let Some(ref v) = self.upload_protocol {
            write!(
                f,
                "&upload_protocol={}",
                percent_encode(format!("{}", v).as_bytes(), NON_ALPHANUMERIC).to_string()
            )?;
        }
        Ok(())
    }
}

/// The Fitness Users service represents the Users resource.
pub struct UsersService {
    client: TlsClient,
    authenticator: Box<dyn 'static + DerefAuth>,
    scopes: Vec<String>,

    base_url: String,
    root_url: String,
}

impl UsersService {
    /// Create a new UsersService object. The easiest way to call this is wrapping the Authenticator
    /// into an `Arc`: `new(client.clone(), Arc::new(authenticator))`.
    /// This way, one authenticator can be shared among several services.
    pub fn new<A: 'static + DerefAuth>(client: TlsClient, auth: A) -> UsersService {
        UsersService {
            client: client,
            authenticator: Box::new(auth),
            scopes: vec![],
            base_url: "https://fitness.googleapis.com/fitness/v1/users/".into(),
            root_url: "https://fitness.googleapis.com/".into(),
        }
    }

    /// Provide the base URL of this API. The returned URL is guaranteed to end with a '/'.
    fn base_url(&self) -> String {
        if self.base_url.ends_with("/") {
            return self.base_url.clone();
        }
        return self.base_url.clone() + "/";
    }
    /// Provide the root URL of this API. The returned URL is guaranteed to end with a '/'.
    fn root_url(&self) -> String {
        if self.root_url.ends_with("/") {
            return self.root_url.clone();
        }
        return self.root_url.clone();
    }
    /// Returns appropriate URLs for relative and absolute paths.
    fn format_path(&self, path: &str) -> String {
        if path.starts_with("/") {
            return self.root_url().trim_end_matches("/").to_string() + path;
        } else {
            return self.base_url() + path;
        }
    }

    #[cfg(test)]
    /// Override API URLs. `base` is the base path relative to which (relative) method paths are interpreted,
    /// whereas `root` is the URL relative to which absolute paths are interpreted.
    pub fn set_urls(&mut self, base: String, root: String) {
        self.base_url = base;
        self.root_url = root;
    }

    /// Explicitly select which scopes should be requested for authorization. Otherwise,
    /// a possibly too large scope will be requested.
    ///
    /// It is most convenient to supply a vec or slice of FitnessScopes enum values.
    pub fn set_scopes<S: AsRef<str>, T: AsRef<[S]>>(&mut self, scopes: T) {
        self.scopes = scopes
            .as_ref()
            .into_iter()
            .map(|s| s.as_ref().to_string())
            .collect();
    }
}

/// The Fitness UsersDataSources service represents the UsersDataSources resource.
pub struct UsersDataSourcesService {
    client: TlsClient,
    authenticator: Box<dyn 'static + DerefAuth>,
    scopes: Vec<String>,

    base_url: String,
    root_url: String,
}

impl UsersDataSourcesService {
    /// Create a new UsersDataSourcesService object. The easiest way to call this is wrapping the Authenticator
    /// into an `Arc`: `new(client.clone(), Arc::new(authenticator))`.
    /// This way, one authenticator can be shared among several services.
    pub fn new<A: 'static + DerefAuth>(client: TlsClient, auth: A) -> UsersDataSourcesService {
        UsersDataSourcesService {
            client: client,
            authenticator: Box::new(auth),
            scopes: vec![],
            base_url: "https://fitness.googleapis.com/fitness/v1/users/".into(),
            root_url: "https://fitness.googleapis.com/".into(),
        }
    }

    /// Provide the base URL of this API. The returned URL is guaranteed to end with a '/'.
    fn base_url(&self) -> String {
        if self.base_url.ends_with("/") {
            return self.base_url.clone();
        }
        return self.base_url.clone() + "/";
    }
    /// Provide the root URL of this API. The returned URL is guaranteed to end with a '/'.
    fn root_url(&self) -> String {
        if self.root_url.ends_with("/") {
            return self.root_url.clone();
        }
        return self.root_url.clone();
    }
    /// Returns appropriate URLs for relative and absolute paths.
    fn format_path(&self, path: &str) -> String {
        if path.starts_with("/") {
            return self.root_url().trim_end_matches("/").to_string() + path;
        } else {
            return self.base_url() + path;
        }
    }

    #[cfg(test)]
    /// Override API URLs. `base` is the base path relative to which (relative) method paths are interpreted,
    /// whereas `root` is the URL relative to which absolute paths are interpreted.
    pub fn set_urls(&mut self, base: String, root: String) {
        self.base_url = base;
        self.root_url = root;
    }

    /// Explicitly select which scopes should be requested for authorization. Otherwise,
    /// a possibly too large scope will be requested.
    ///
    /// It is most convenient to supply a vec or slice of FitnessScopes enum values.
    pub fn set_scopes<S: AsRef<str>, T: AsRef<[S]>>(&mut self, scopes: T) {
        self.scopes = scopes
            .as_ref()
            .into_iter()
            .map(|s| s.as_ref().to_string())
            .collect();
    }

    /// Creates a new data source that is unique across all data sources belonging to this user. A data source is a unique source of sensor data. Data sources can expose raw data coming from hardware sensors on local or companion devices. They can also expose derived data, created by transforming or merging other data sources. Multiple data sources can exist for the same data type. Every data point in every dataset inserted into or read from the Fitness API has an associated data source. Each data source produces a unique stream of dataset updates, with a unique data source identifier. Not all changes to data source affect the data stream ID, so that data collected by updated versions of the same application/device can still be considered to belong to the same data source. Data sources are identified using a string generated by the server, based on the contents of the source being created. The dataStreamId field should not be set when invoking this method. It will be automatically generated by the server with the correct format. If a dataStreamId is set, it must match the format that the server would generate. This format is a combination of some fields from the data source, and has a specific order. If it doesn't match, the request will fail with an error. Specifying a DataType which is not a known type (beginning with "com.google.") will create a DataSource with a *custom data type*. Custom data types are only readable by the application that created them. Custom data types are *deprecated*; use standard data types instead. In addition to the data source fields included in the data source ID, the developer project number that is authenticated when creating the data source is included. This developer project number is obfuscated when read by any other developer reading public data types.
    pub async fn create(
        &self,
        params: &UsersDataSourcesCreateParams,
        req: &DataSource,
    ) -> Result<DataSource> {
        let rel_path = format!(
            "{userId}/dataSources",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC)
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        let opt_request = Some(req);
        do_request(&self.client, &full_uri, &headers, "POST", opt_request).await
    }

    /// Deletes the specified data source. The request will fail if the data source contains any data points.
    pub async fn delete(&self, params: &UsersDataSourcesDeleteParams) -> Result<DataSource> {
        let rel_path = format!(
            "{userId}/dataSources/{dataSourceId}",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC),
            dataSourceId = percent_encode(
                format!("{}", params.data_source_id).as_bytes(),
                NON_ALPHANUMERIC
            )
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        do_request(&self.client, &full_uri, &headers, "DELETE", opt_request).await
    }

    /// Returns the specified data source.
    pub async fn get(&self, params: &UsersDataSourcesGetParams) -> Result<DataSource> {
        let rel_path = format!(
            "{userId}/dataSources/{dataSourceId}",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC),
            dataSourceId = percent_encode(
                format!("{}", params.data_source_id).as_bytes(),
                NON_ALPHANUMERIC
            )
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        do_request(&self.client, &full_uri, &headers, "GET", opt_request).await
    }

    /// Lists all data sources that are visible to the developer, using the OAuth scopes provided. The list is not exhaustive; the user may have private data sources that are only visible to other developers, or calls using other scopes.
    pub async fn list(
        &self,
        params: &UsersDataSourcesListParams,
    ) -> Result<ListDataSourcesResponse> {
        let rel_path = format!(
            "{userId}/dataSources",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC)
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        do_request(&self.client, &full_uri, &headers, "GET", opt_request).await
    }

    /// Updates the specified data source. The dataStreamId, dataType, type, dataStreamName, and device properties with the exception of version, cannot be modified. Data sources are identified by their dataStreamId.
    pub async fn update(
        &self,
        params: &UsersDataSourcesUpdateParams,
        req: &DataSource,
    ) -> Result<DataSource> {
        let rel_path = format!(
            "{userId}/dataSources/{dataSourceId}",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC),
            dataSourceId = percent_encode(
                format!("{}", params.data_source_id).as_bytes(),
                NON_ALPHANUMERIC
            )
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        let opt_request = Some(req);
        do_request(&self.client, &full_uri, &headers, "PUT", opt_request).await
    }
}

/// The Fitness UsersDataSourcesDataPointChanges service represents the UsersDataSourcesDataPointChanges resource.
pub struct UsersDataSourcesDataPointChangesService {
    client: TlsClient,
    authenticator: Box<dyn 'static + DerefAuth>,
    scopes: Vec<String>,

    base_url: String,
    root_url: String,
}

impl UsersDataSourcesDataPointChangesService {
    /// Create a new UsersDataSourcesDataPointChangesService object. The easiest way to call this is wrapping the Authenticator
    /// into an `Arc`: `new(client.clone(), Arc::new(authenticator))`.
    /// This way, one authenticator can be shared among several services.
    pub fn new<A: 'static + DerefAuth>(
        client: TlsClient,
        auth: A,
    ) -> UsersDataSourcesDataPointChangesService {
        UsersDataSourcesDataPointChangesService {
            client: client,
            authenticator: Box::new(auth),
            scopes: vec![],
            base_url: "https://fitness.googleapis.com/fitness/v1/users/".into(),
            root_url: "https://fitness.googleapis.com/".into(),
        }
    }

    /// Provide the base URL of this API. The returned URL is guaranteed to end with a '/'.
    fn base_url(&self) -> String {
        if self.base_url.ends_with("/") {
            return self.base_url.clone();
        }
        return self.base_url.clone() + "/";
    }
    /// Provide the root URL of this API. The returned URL is guaranteed to end with a '/'.
    fn root_url(&self) -> String {
        if self.root_url.ends_with("/") {
            return self.root_url.clone();
        }
        return self.root_url.clone();
    }
    /// Returns appropriate URLs for relative and absolute paths.
    fn format_path(&self, path: &str) -> String {
        if path.starts_with("/") {
            return self.root_url().trim_end_matches("/").to_string() + path;
        } else {
            return self.base_url() + path;
        }
    }

    #[cfg(test)]
    /// Override API URLs. `base` is the base path relative to which (relative) method paths are interpreted,
    /// whereas `root` is the URL relative to which absolute paths are interpreted.
    pub fn set_urls(&mut self, base: String, root: String) {
        self.base_url = base;
        self.root_url = root;
    }

    /// Explicitly select which scopes should be requested for authorization. Otherwise,
    /// a possibly too large scope will be requested.
    ///
    /// It is most convenient to supply a vec or slice of FitnessScopes enum values.
    pub fn set_scopes<S: AsRef<str>, T: AsRef<[S]>>(&mut self, scopes: T) {
        self.scopes = scopes
            .as_ref()
            .into_iter()
            .map(|s| s.as_ref().to_string())
            .collect();
    }

    /// Queries for user's data point changes for a particular data source.
    pub async fn list(
        &self,
        params: &UsersDataSourcesDataPointChangesListParams,
    ) -> Result<ListDataPointChangesResponse> {
        let rel_path = format!(
            "{userId}/dataSources/{dataSourceId}/dataPointChanges",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC),
            dataSourceId = percent_encode(
                format!("{}", params.data_source_id).as_bytes(),
                NON_ALPHANUMERIC
            )
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        do_request(&self.client, &full_uri, &headers, "GET", opt_request).await
    }
}

/// The Fitness UsersDataSourcesDatasets service represents the UsersDataSourcesDatasets resource.
pub struct UsersDataSourcesDatasetsService {
    client: TlsClient,
    authenticator: Box<dyn 'static + DerefAuth>,
    scopes: Vec<String>,

    base_url: String,
    root_url: String,
}

impl UsersDataSourcesDatasetsService {
    /// Create a new UsersDataSourcesDatasetsService object. The easiest way to call this is wrapping the Authenticator
    /// into an `Arc`: `new(client.clone(), Arc::new(authenticator))`.
    /// This way, one authenticator can be shared among several services.
    pub fn new<A: 'static + DerefAuth>(
        client: TlsClient,
        auth: A,
    ) -> UsersDataSourcesDatasetsService {
        UsersDataSourcesDatasetsService {
            client: client,
            authenticator: Box::new(auth),
            scopes: vec![],
            base_url: "https://fitness.googleapis.com/fitness/v1/users/".into(),
            root_url: "https://fitness.googleapis.com/".into(),
        }
    }

    /// Provide the base URL of this API. The returned URL is guaranteed to end with a '/'.
    fn base_url(&self) -> String {
        if self.base_url.ends_with("/") {
            return self.base_url.clone();
        }
        return self.base_url.clone() + "/";
    }
    /// Provide the root URL of this API. The returned URL is guaranteed to end with a '/'.
    fn root_url(&self) -> String {
        if self.root_url.ends_with("/") {
            return self.root_url.clone();
        }
        return self.root_url.clone();
    }
    /// Returns appropriate URLs for relative and absolute paths.
    fn format_path(&self, path: &str) -> String {
        if path.starts_with("/") {
            return self.root_url().trim_end_matches("/").to_string() + path;
        } else {
            return self.base_url() + path;
        }
    }

    #[cfg(test)]
    /// Override API URLs. `base` is the base path relative to which (relative) method paths are interpreted,
    /// whereas `root` is the URL relative to which absolute paths are interpreted.
    pub fn set_urls(&mut self, base: String, root: String) {
        self.base_url = base;
        self.root_url = root;
    }

    /// Explicitly select which scopes should be requested for authorization. Otherwise,
    /// a possibly too large scope will be requested.
    ///
    /// It is most convenient to supply a vec or slice of FitnessScopes enum values.
    pub fn set_scopes<S: AsRef<str>, T: AsRef<[S]>>(&mut self, scopes: T) {
        self.scopes = scopes
            .as_ref()
            .into_iter()
            .map(|s| s.as_ref().to_string())
            .collect();
    }

    /// Performs an inclusive delete of all data points whose start and end times have any overlap with the time range specified by the dataset ID. For most data types, the entire data point will be deleted. For data types where the time span represents a consistent value (such as com.google.activity.segment), and a data point straddles either end point of the dataset, only the overlapping portion of the data point will be deleted.
    pub async fn delete(&self, params: &UsersDataSourcesDatasetsDeleteParams) -> Result<()> {
        let rel_path = format!(
            "{userId}/dataSources/{dataSourceId}/datasets/{datasetId}",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC),
            dataSourceId = percent_encode(
                format!("{}", params.data_source_id).as_bytes(),
                NON_ALPHANUMERIC
            ),
            datasetId = percent_encode(
                format!("{}", params.dataset_id).as_bytes(),
                NON_ALPHANUMERIC
            )
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        do_request(&self.client, &full_uri, &headers, "DELETE", opt_request).await
    }

    /// Returns a dataset containing all data points whose start and end times overlap with the specified range of the dataset minimum start time and maximum end time. Specifically, any data point whose start time is less than or equal to the dataset end time and whose end time is greater than or equal to the dataset start time.
    pub async fn get(&self, params: &UsersDataSourcesDatasetsGetParams) -> Result<Dataset> {
        let rel_path = format!(
            "{userId}/dataSources/{dataSourceId}/datasets/{datasetId}",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC),
            dataSourceId = percent_encode(
                format!("{}", params.data_source_id).as_bytes(),
                NON_ALPHANUMERIC
            ),
            datasetId = percent_encode(
                format!("{}", params.dataset_id).as_bytes(),
                NON_ALPHANUMERIC
            )
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        do_request(&self.client, &full_uri, &headers, "GET", opt_request).await
    }

    /// Adds data points to a dataset. The dataset need not be previously created. All points within the given dataset will be returned with subsquent calls to retrieve this dataset. Data points can belong to more than one dataset. This method does not use patch semantics: the data points provided are merely inserted, with no existing data replaced.
    pub async fn patch(
        &self,
        params: &UsersDataSourcesDatasetsPatchParams,
        req: &Dataset,
    ) -> Result<Dataset> {
        let rel_path = format!(
            "{userId}/dataSources/{dataSourceId}/datasets/{datasetId}",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC),
            dataSourceId = percent_encode(
                format!("{}", params.data_source_id).as_bytes(),
                NON_ALPHANUMERIC
            ),
            datasetId = percent_encode(
                format!("{}", params.dataset_id).as_bytes(),
                NON_ALPHANUMERIC
            )
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        let opt_request = Some(req);
        do_request(&self.client, &full_uri, &headers, "PATCH", opt_request).await
    }
}

/// The Fitness UsersDataset service represents the UsersDataset resource.
pub struct UsersDatasetService {
    client: TlsClient,
    authenticator: Box<dyn 'static + DerefAuth>,
    scopes: Vec<String>,

    base_url: String,
    root_url: String,
}

impl UsersDatasetService {
    /// Create a new UsersDatasetService object. The easiest way to call this is wrapping the Authenticator
    /// into an `Arc`: `new(client.clone(), Arc::new(authenticator))`.
    /// This way, one authenticator can be shared among several services.
    pub fn new<A: 'static + DerefAuth>(client: TlsClient, auth: A) -> UsersDatasetService {
        UsersDatasetService {
            client: client,
            authenticator: Box::new(auth),
            scopes: vec![],
            base_url: "https://fitness.googleapis.com/fitness/v1/users/".into(),
            root_url: "https://fitness.googleapis.com/".into(),
        }
    }

    /// Provide the base URL of this API. The returned URL is guaranteed to end with a '/'.
    fn base_url(&self) -> String {
        if self.base_url.ends_with("/") {
            return self.base_url.clone();
        }
        return self.base_url.clone() + "/";
    }
    /// Provide the root URL of this API. The returned URL is guaranteed to end with a '/'.
    fn root_url(&self) -> String {
        if self.root_url.ends_with("/") {
            return self.root_url.clone();
        }
        return self.root_url.clone();
    }
    /// Returns appropriate URLs for relative and absolute paths.
    fn format_path(&self, path: &str) -> String {
        if path.starts_with("/") {
            return self.root_url().trim_end_matches("/").to_string() + path;
        } else {
            return self.base_url() + path;
        }
    }

    #[cfg(test)]
    /// Override API URLs. `base` is the base path relative to which (relative) method paths are interpreted,
    /// whereas `root` is the URL relative to which absolute paths are interpreted.
    pub fn set_urls(&mut self, base: String, root: String) {
        self.base_url = base;
        self.root_url = root;
    }

    /// Explicitly select which scopes should be requested for authorization. Otherwise,
    /// a possibly too large scope will be requested.
    ///
    /// It is most convenient to supply a vec or slice of FitnessScopes enum values.
    pub fn set_scopes<S: AsRef<str>, T: AsRef<[S]>>(&mut self, scopes: T) {
        self.scopes = scopes
            .as_ref()
            .into_iter()
            .map(|s| s.as_ref().to_string())
            .collect();
    }

    /// Aggregates data of a certain type or stream into buckets divided by a given type of boundary. Multiple data sets of multiple types and from multiple sources can be aggregated into exactly one bucket type per request.
    pub async fn aggregate(
        &self,
        params: &UsersDatasetAggregateParams,
        req: &AggregateRequest,
    ) -> Result<AggregateResponse> {
        let rel_path = format!(
            "{userId}/dataset:aggregate",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC)
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        let opt_request = Some(req);
        do_request(&self.client, &full_uri, &headers, "POST", opt_request).await
    }
}

/// The Fitness UsersSessions service represents the UsersSessions resource.
pub struct UsersSessionsService {
    client: TlsClient,
    authenticator: Box<dyn 'static + DerefAuth>,
    scopes: Vec<String>,

    base_url: String,
    root_url: String,
}

impl UsersSessionsService {
    /// Create a new UsersSessionsService object. The easiest way to call this is wrapping the Authenticator
    /// into an `Arc`: `new(client.clone(), Arc::new(authenticator))`.
    /// This way, one authenticator can be shared among several services.
    pub fn new<A: 'static + DerefAuth>(client: TlsClient, auth: A) -> UsersSessionsService {
        UsersSessionsService {
            client: client,
            authenticator: Box::new(auth),
            scopes: vec![],
            base_url: "https://fitness.googleapis.com/fitness/v1/users/".into(),
            root_url: "https://fitness.googleapis.com/".into(),
        }
    }

    /// Provide the base URL of this API. The returned URL is guaranteed to end with a '/'.
    fn base_url(&self) -> String {
        if self.base_url.ends_with("/") {
            return self.base_url.clone();
        }
        return self.base_url.clone() + "/";
    }
    /// Provide the root URL of this API. The returned URL is guaranteed to end with a '/'.
    fn root_url(&self) -> String {
        if self.root_url.ends_with("/") {
            return self.root_url.clone();
        }
        return self.root_url.clone();
    }
    /// Returns appropriate URLs for relative and absolute paths.
    fn format_path(&self, path: &str) -> String {
        if path.starts_with("/") {
            return self.root_url().trim_end_matches("/").to_string() + path;
        } else {
            return self.base_url() + path;
        }
    }

    #[cfg(test)]
    /// Override API URLs. `base` is the base path relative to which (relative) method paths are interpreted,
    /// whereas `root` is the URL relative to which absolute paths are interpreted.
    pub fn set_urls(&mut self, base: String, root: String) {
        self.base_url = base;
        self.root_url = root;
    }

    /// Explicitly select which scopes should be requested for authorization. Otherwise,
    /// a possibly too large scope will be requested.
    ///
    /// It is most convenient to supply a vec or slice of FitnessScopes enum values.
    pub fn set_scopes<S: AsRef<str>, T: AsRef<[S]>>(&mut self, scopes: T) {
        self.scopes = scopes
            .as_ref()
            .into_iter()
            .map(|s| s.as_ref().to_string())
            .collect();
    }

    /// Deletes a session specified by the given session ID.
    pub async fn delete(&self, params: &UsersSessionsDeleteParams) -> Result<()> {
        let rel_path = format!(
            "{userId}/sessions/{sessionId}",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC),
            sessionId = percent_encode(
                format!("{}", params.session_id).as_bytes(),
                NON_ALPHANUMERIC
            )
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        do_request(&self.client, &full_uri, &headers, "DELETE", opt_request).await
    }

    /// Lists sessions previously created.
    pub async fn list(&self, params: &UsersSessionsListParams) -> Result<ListSessionsResponse> {
        let rel_path = format!(
            "{userId}/sessions",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC)
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        do_request(&self.client, &full_uri, &headers, "GET", opt_request).await
    }

    /// Updates or insert a given session.
    pub async fn update(
        &self,
        params: &UsersSessionsUpdateParams,
        req: &Session,
    ) -> Result<Session> {
        let rel_path = format!(
            "{userId}/sessions/{sessionId}",
            userId = percent_encode(format!("{}", params.user_id).as_bytes(), NON_ALPHANUMERIC),
            sessionId = percent_encode(
                format!("{}", params.session_id).as_bytes(),
                NON_ALPHANUMERIC
            )
        );
        let path = self.format_path(rel_path.as_str());

        let mut headers = vec![];
        let tok;
        if self.scopes.is_empty() {
            let scopes = &[FitnessScopes::FitnessSleepWrite.as_ref().to_string()];
            tok = self.authenticator.token(scopes).await?;
        } else {
            tok = self.authenticator.token(&self.scopes).await?;
        }
        headers.push((
            hyper::header::AUTHORIZATION,
            format!("Bearer {token}", token = tok.as_str()),
        ));

        let mut url_params = format!("?{params}", params = params);
        if let Some(ref api_params) = &params.fitness_params {
            url_params.push_str(&format!("{}", api_params));
        }

        let full_uri = path + &url_params;

        let opt_request: Option<&EmptyRequest> = None;
        let opt_request = Some(req);
        do_request(&self.client, &full_uri, &headers, "PUT", opt_request).await
    }
}
